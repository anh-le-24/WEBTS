﻿@{
    ViewBag.Title = "Điểm Tích Lũy";
    Layout = "~/Views/Shared/_TrangCaNhanLayout.cshtml";
}

<div class="loyalty-container">
    <h1>Điểm Tích Lũy</h1>

    @foreach (var d in ViewBag.list)
    {
        <div class="loyalty-summary">
            <div class="summary-item">
                <strong>Tổng số đơn hàng đã hoàn thành:</strong>
                <span>@d[1] đơn hàng</span>
            </div>
            <div class="summary-item">
                <strong>Tổng số tiền đã chi:</strong>
                <span>@d[2] VND</span>
            </div>
            <div class="summary-item">
                <strong>Điểm tích lũy:</strong>
                <span>@d[0] điểm</span>
            </div>
        </div>
    }

    <div class="loyalty-info">
        <h2>Lợi ích của điểm tích lũy</h2>
        <ul>
            <li>Sử dụng để giảm giá tiền hoặc ưu đãi đặc biệt.</li>
            <li>Mỗi 10,000 VNĐ chi tiêu = 1 điểm tích lũy.</li>
            <li>Điểm tích lũy không có giá trị quy đổi thành tiền mặt.</li>
        </ul>
    </div>
</div>

<style>
    /* Main container */
    .loyalty-container {
        max-width: 900px;
        margin: 40px auto;
        padding: 30px;
        background: linear-gradient(135deg, #f3f4f7, #e6e8eb);
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        font-family: 'Arial', sans-serif;
    }

    h1 {
        font-size: 28px;
        color: #333;
        text-align: center;
        margin-bottom: 20px;
        font-weight: 600;
    }

    /* Summary Section */
    .loyalty-summary {
        background-color: #ffffff;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        margin-bottom: 30px;
    }

    .summary-item {
        display: flex;
        justify-content: space-between;
        font-size: 18px;
        color: #555;
        margin-bottom: 15px;
        padding: 10px;
        background-color: #f9f9f9;
        border-radius: 8px;
    }

        .summary-item strong {
            color: #333;
            font-weight: 500;
        }

        .summary-item span {
            font-weight: 600;
            color: #007bff;
        }

    /* Information Section */
    .loyalty-info {
        background-color: #ffffff;
        padding: 25px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

        .loyalty-info h2 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #333;
            font-weight: 600;
        }

        .loyalty-info ul {
            list-style-type: disc;
            padding-left: 20px;
        }

            .loyalty-info ul li {
                font-size: 16px;
                color: #555;
                margin: 8px 0;
            }
</style>